## Packages
framer-motion | Smooth transitions for multi-step form and page elements
@hookform/resolvers | Zod resolver for react-hook-form
zod | Schema validation
lucide-react | Beautiful icons

## Notes
Backend endpoint POST /api/leads returns { redirectUrl: string }
Form has 3 steps: 
1. Loan Amount & Purpose
2. Credit Score & Employment
3. Contact Info (Zip, Email, Phone)
Redirect user immediately upon successful submission.
